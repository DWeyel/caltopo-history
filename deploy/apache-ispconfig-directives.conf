# Paste into ISPConfig -> Sites -> Website -> Options -> Apache Directives
# Requires mod_proxy + mod_proxy_http (normally available on ISPConfig web servers).
ProxyPreserveHost On
ProxyPass / http://127.0.0.1:8765/
ProxyPassReverse / http://127.0.0.1:8765/
