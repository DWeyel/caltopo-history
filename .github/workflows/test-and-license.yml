name: Test and license audit

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-24.04
    steps:
      - name: CI maintenance pause
        run: echo "Automatic CI is temporarily paused while historical test fixtures are normalized."
